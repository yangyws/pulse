# Agent Instructions & Guidelines

## 🛡️ Git 安全推送規範 (Git Push Protection)

* **嚴格禁止私自執行 `git push`**：
  在任何情況下，**絕對不得未經使用者明確確認而自動執行 `git push`**。
* **推送前確認流程**：
  1. 本地允許進行 `git add`、`git commit`、`git status`、`git diff` 等操作。
  2. 若有準備推送的新 commit，必須先向使用者匯報修改摘要，並主動詢問：「請問是否確認將上述修改推送到遠端 GitHub 儲存庫？」。
  3. **必須停下來等待使用者給出明確確認訊息**（如「確認」、「推上去」、「同意」等），才可執行推送指令。

## 🇹🇼 繁體中文與台灣用語規範 (Taiwan Traditional Chinese Standard)

* **一律使用台灣繁體中文 (zh-TW)**：
  在任何對話回覆、程式碼本地化、註解、UI 介面文字或輸出訊息中，只要涉及中文，**一律嚴格使用台灣繁體中文語系與用語風格**。
* **嚴格禁止使用中國大陸簡體字與大陸用語**：
  * **絕對不得出現任何簡體中文字**。
  * **嚴格禁止使用中國大陸習慣用語**（如「项目」、「代码」、「仓库」、「默认」、「支持」、「服务器」、「内存」、「屏幕」等），必須轉換為台灣常見科技標準用語（如「專案」、「程式碼」、「儲存庫」、「預設」、「支援」、「伺服器」、「記憶體」、「螢幕」等）。

